fn main() {
    
    // Using Vec::new()
    let v : Vec<i64> = Vec::new();

    // printing the size of the vector
    println!("\nThe lenght of the Vec::new() is: {}",v.len());

    // Using marco
    let v = vec!["Grace","Effiong","Basil","Kareem","Susan"];

    // printing the size of the vector
    println!("\nThe lenght vec marco is: { }",v.len());
}
